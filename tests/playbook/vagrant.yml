---
- hosts: all
  become: yes
  become_user: root
  roles:
  - role: ../../prometheus-node-exporter
    prometheus_global_labels:
      vars:
        prometheus_node_exporter_enabled_collectors:
          - gmond
          - loadavg
      tags:
        - node-exporter
